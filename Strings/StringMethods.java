import java.util.*;
public class StringMethods {
    public static void main(String[] args){
        String str = "Jeeya Sharma";

        // toCharArray() method converts the string to character array
        System.out.println(Arrays.toString(str.toCharArray()));

        // toLowerCase() method converts the whole string into lowercase
        System.out.println(str.toLowerCase());
    }
}
